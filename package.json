{
  "name": "exploringtoknow",
  "version": "0.1.0",
  "private": true,
  "description": "ExploringToKnow — Autonomous Affiliate & Commerce Content Engine (internal, single-tenant)",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules",
    "web": "pnpm --filter @etk/web",
    "worker": "pnpm --filter @etk/worker"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "embedded-postgres": "18.4.0-beta.17",
    "prettier": "^3.3.3",
    "tsx": "^4.19.1",
    "turbo": "^2.1.3",
    "typescript": "^5.6.3"
  }
}
