# @etk/tracking

Tracking link builder, UTM, /go/:code resolution + click logging (Phase 2). NOT LangGraph.

Placeholder in Phase 0 — directory reserved to keep the repo structure aligned
with implementation package §2. Real package.json + src/ added in the phase noted
above.
